export const tileSide = 100;
export const tileWidth = 2 * Math.sqrt(Math.pow(tileSide, 2) - Math.pow(tileSide / 2, 2));
export const tileRoofHeight = Math.sqrt(Math.pow(tileSide, 2) - Math.pow(tileWidth / 2, 2));
