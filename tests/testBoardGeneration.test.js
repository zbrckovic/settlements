import { createBoard } from '../src/game/board'
import { generateBoard } from '../src/index'


describe('board', () => {
    let testBoard
});

test('rotate', () => {
    const expectedBoard = createBoard({
        tiles: [generateBoard()]
    })
});